{
  "setup": [
    "cd web && bun install && cd ../api && bun install && cd ../ && cp \"$SUPERSET_ROOT_PATH/web/.env\" web/.env && cp \"$SUPERSET_ROOT_PATH/api/.env\" api/.env"
  ],
  "teardown": [
    "cd web && rm -rf node_modules && cd ../api && rm -rf node_modules"
  ]
}
